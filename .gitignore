__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.roadkeep/
# The documentation area's build output (RK1398). Its source is site/ and it lands inside the
# governed store's one reserved subtree; it is generated by the workflow that publishes it, so
# a committed copy would be a second answer to what the site is — the staleness the derived
# README block already has a gate against.
docs/guide/
