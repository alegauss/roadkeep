# Turing — Roadmap (a frozen excerpt)

> **Not a backlog.** One line, copied byte for byte out of Turing's own
> `docs/ROADMAP.md` at `f08304fcb1`, because the shape it carries — an **open line
> pointing at an anchor two prose files both declare** — is what RK1144's re-pin
> retired: the thirteen doubled anchors survive at `2d71c9eac9` and this line does
> not, so the state `section.ambiguous` is about was left read off no backlog.
>
> The provenance is prose on purpose: the parser reads task lines and ignores this.

## Block O — GEO / LLM discoverability & content distribution

- 📋 **T354** (deps: —) **Seed independent mentions** — answer relevant SO/Reddit/AEM-forum threads, republish the posts to Dev.to/Medium behind a canonical link, and list on AlternativeTo, `awesome-*` and G2. → §X.1
