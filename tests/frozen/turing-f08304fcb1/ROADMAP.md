# Turing — Roadmap (a frozen excerpt)

> **Not a backlog.** One line, copied byte for byte out of Turing's own `docs/ROADMAP.md`
> at `f08304fcb1`, because the shape it carries — a **range dep**, `T451–T457` with an
> en dash — shipped out of the live tree and RK1144's re-pin retired the only evidence
> that it was ever real (RK1145).
>
> The provenance is prose on purpose: the parser reads task lines and ignores this.

## Block BQ — The demo

- ⏳ **T477** (deps: T451–T457 ✅) **Public read-only demo instance behind the site** — infra, seeding and the LLM are live at `turing-demo.viglet.org`, so what is left is the host setting the demo agent's budget cap. → §XXVII.2
